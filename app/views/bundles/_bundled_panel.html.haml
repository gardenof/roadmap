%td.panel
  .panelHeaderRight
  .panelHeaderLeft
  .panelHeader
    %span.panelTitle BUNDLED (#{@bundled_features.count})

  - @bundled_features.each do |feature|
    .collapsed-feature{class: tracker_status_class(feature)}
      %span.feature_name
      = link_to feature.name, [@project, feature]
      .operations
        = form_tag remove_feature_project_bundle_path(@project,@bundle) do |f|
          = hidden_field_tag :feature_id, feature.id
          = submit_tag '-'
      .status-tags
        - if feature.updatable?
          = form_tag(project_schedule_path(@project), method: :post) do
            = hidden_field_tag 'feature_id', feature.id
            = submit_tag 'Schedule Feature',
            confirm: 'Are you sure?  This is synchronous for the moment'
        - else
          = feature.current_state
